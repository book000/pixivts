{
  "name": "pixivts-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "test": "vitest run",
    "lint": "pnpm --filter @book000/pixivts build && pnpm -r lint && eslint .",
    "fix": "pnpm -r fix && eslint . --fix"
  },
  "devDependencies": {
    "@book000/eslint-config": "1.15.2",
    "@types/node": "24.13.2",
    "eslint": "10.4.1",
    "prettier": "3.8.4",
    "rimraf": "6.1.3",
    "typescript": "6.0.3",
    "vitest": "3.2.4"
  },
  "packageManager": "pnpm@11.6.0"
}
